# dependencies
node_modules/

# build & package artifacts
dist/
*.tgz

# runtime output (generated daily reports, uploaded material)
daily-reports/

# logs & scratch
*.log
*.tmp

# local pre-fix backup (user's own snapshot, keep off the repo)
.pre-fix-backup/
